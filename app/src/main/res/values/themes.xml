<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TODO" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- Primary color (top and bottom bars) -->
        <item name="colorPrimary">@color/color_top_bottom_bar</item>



        <!-- Floating Action Button color -->
        <item name="colorSecondary">@color/color_fab</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/color_status_bar</item>

        <!-- Text colors -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:textColorPrimary">@color/white</item>

        <!-- Background color -->
        <item name="android:colorBackground">@color/color_background</item>

        <item name="android:navigationBarColor">@color/white</item>


    </style>


    <style name="Theme.TODO" parent="Base.Theme.TODO" />


    <style name="AppDatePickerDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <!-- DatePickerDialog için özelleştirmelerinizi burada yapabilirsiniz -->
        <item name="colorAccent">@color/color_fab</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Diğer özellikler -->
    </style>

    <style name="AppTimePickerDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <!-- TimePickerDialog için özelleştirmelerinizi burada yapabilirsiniz -->
        <item name="colorAccent">@color/color_fab</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Diğer özellikler -->
    </style>


</resources>